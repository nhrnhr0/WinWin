{
    "version": "0.2.0",
    "configurations": [{
        "name": "Launch strapi develop",
        "cwd": "${workspaceRoot}/backend",
        "request": "launch",
        "type": "node-terminal",
        "command": "yarn run strapi develop --watch-admin",
    }]
}