'use strict';

/**
 * institution-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::institution-category.institution-category');
